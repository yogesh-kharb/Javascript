// control flow


// if

if(true){
    // this will be executed if true is the final cond in if is true

}

// eg if we check if the user is logged in or not
const isUserLoggedIn = false;
// if we want to confirm the datatype also then we use the
// === operator and for normal equality we use the ==
// while if we do the assignment then we will use the =

if(isUserLoggedIn){
    // this will be executed only if we have the a logged in user

}


