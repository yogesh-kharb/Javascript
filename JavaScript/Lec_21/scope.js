// curly braces are generally the scope in almost all programming languages

// the story of scope starts with the var , let , const


// if we declare a variable name inside a function then let , const
// are never accessible outside the function but the var creates
// a problem and it can be accessed outside the function



// see the following story
// dhanno will not be  satisfied
// the var we write inside the blocks is local scope and taht declared globaly is global scope


// global scope can be diff for node local environment and for the console on browser